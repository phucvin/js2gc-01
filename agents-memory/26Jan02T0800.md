Summary:
- Implementing binaryen roundtrip in compiler pipeline (validation, no optimization).
- Removing optimization from `scripts/run_scratch_use_wat.ts`.

# User Prompt
validate wat (but do not optimize) and do roundtrip through binaryen before return final wat

also follow agents.md

# Agent Response
I will modify `src/compiler/index.ts` to include a Binaryen roundtrip (parse -> validate -> emit) without optimization before returning the WAT string. I will also remove `module.optimize()` from `scripts/run_scratch_use_wat.ts`.
