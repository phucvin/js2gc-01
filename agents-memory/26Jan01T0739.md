Summary: Initial plan to add Wasm GC examples and remove console.log.

# User Prompt
add 4 wasm gc examples to binaryen api example

remove console.log in binaryen api examples

also follow agents.md

# Agent Response
I will add 4 Wasm GC examples to `binary_api_examples/` using `binaryen.parseText` to avoid API issues, and I will remove `console.log` from all files in `binary_api_examples/`. I will also follow the `AGENTS.md` by creating this log file.
