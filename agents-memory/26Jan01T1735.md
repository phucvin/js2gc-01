Summary: Moving all run.ts scripts to scripts/ directory, fixing them to work from root, and updating READMEs.

# User Prompt
move all run.ts scripts to be under scripts at repo root, named accordingly

fix scripts so they know they are executed at root, and need to get the correct directory to get input files. run all scripts to update outputs

updated corresponding readme regarding running the script to run & update outputs

also follow agents.md
