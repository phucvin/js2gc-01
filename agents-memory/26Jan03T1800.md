Summary: Removed `stringref` and `enableStringRef` flag, replacing strings with `array i8`.

# User Prompt
remove stringref and flag controlling it

use array i8 for string instead

also follow agents.md

# Agent Response
I removed the `enableStringRef` option from `src/compiler/context.ts` and the associated logic in `src/compiler/index.ts`. I redefined `$String` as `(array (mut i8))` and updated `$BoxedString` to wrap this new type. I modified `src/compiler/expression.ts` to compile string literals into `array.new_fixed` instructions using UTF-8 byte values. I updated `scripts/run_benchmark.ts` and `scripts/run_testdata.ts` to remove the `binaryen.Features.Strings` flag and to handle printing of the new array-based strings (currently printing `[String]` as a placeholder). I verified the changes by running `scripts/run_benchmark.ts` and `scripts/run_testdata.ts`, confirming that compilation and execution proceed correctly.
