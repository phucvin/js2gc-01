Summary: Fixed README.md to use the correct `scripts/run_testdata.ts` runner via `ts-node`, resolving path and execution issues.

# User Prompt

fix readme run testdata path

also follow agents.md

# Agent Response

I have updated `README.md` to point to the correct test runner script `scripts/run_testdata.ts` using `ts-node`, instead of the obsolete `dist/testdata/run.js` method.

Verified the new command works:
`node --experimental-wasm-stringref node_modules/ts-node/dist/bin.js scripts/run_testdata.ts`
