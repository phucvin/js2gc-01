Summary: Added `enableStringRef` option to compiler.

# User Prompt
add options to the compiler to enable stringref (default to true)

if it's disabled, treat string literal as null

also follow agents.md

# Agent Response
I added a `enableStringRef` option to the `CompilerOptions` interface in `src/compiler/context.ts`, defaulting it to `true`. I modified `src/compiler/index.ts` to respect this option by conditionally emitting string-related types, imports, and Binaryen features. I also updated `src/compiler/expression.ts` to check this option when compiling string literals, returning a null reference if disabled. A reproduction test `scratch/test_stringref_option.ts` confirmed the behavior.
