Summary: Implemented missing opcodes and fixed compiler bugs in scratch/wat_interpreter to run testdata/add.wat successfully.

Details:
- Built `scratch/wat_interpreter`.
- Implemented `Op::CallRef`, `Op::RefFunc`, `Op::EqI32`, `Op::AndI32`, `Op::GlobalGet` in `vm.cpp`.
- Added global variable support in `Compiler` and `VM`.
- Fixed `Compiler::parseFuncSig` and `Compiler::compileFunc` to correctly parse named parameters and locals by skipping type annotations.
- Verified output matches `testdata/add.out` (3).

# User Prompt
run testdata add.wat with scratch wat interpreter

also follow agents.md

# Agent Response
I am setting up the session memory and proceeding to verify the files and build the interpreter.
