Summary: New session started. Task: support prototype inheritance and optimize field access using inline cache.

# User Prompt
support prototype inheritance, optimize field access so it still be fast by using inline cache

also follow agents.md

# Agent Response
I will start by analyzing the current implementation of objects and field access in `src/compiler`. Then I will plan the changes to support prototype inheritance and ensure inline caching works efficiently with it.
