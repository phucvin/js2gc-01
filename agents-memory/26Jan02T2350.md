Summary: Added gitignore to wat_interpreter, ran interpreters with testdata, and updated READMEs.

# User Prompt
add gitignore to scratch wat interpreter to ignore the binary

run both interpreter with testdata add .js and .wat and update their readme

also follow agents

# Agent Response
I added `.gitignore` to `scratch/wat_interpreter` to ignore the `wat_interpreter` binary.
I ran `scratch/js_interpreter` with `testdata/add.js` and `scratch/wat_interpreter` with `testdata/add.wat`.
I updated `scratch/js_interpreter/README.md` and `scratch/wat_interpreter/README.md` with the execution commands and outputs.
