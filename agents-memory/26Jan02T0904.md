Summary: Updated `testdata/closure_return.js` to use a top-level function, requiring updates to `src/compiler/function.ts` to handle function parameters in top-level functions. Verified all tests pass.

# User Prompt

make it a top-level function

# Agent Response

I modified `testdata/closure_return.js` to define `makeAdder` as a top-level function. I also updated `src/compiler/function.ts` to correctly compile function parameters for top-level functions (adding them to the context and emitting them in the signature). I verified the changes with `scripts/run_testdata.ts`.
