Summary: Synced `prototype-inline-cache` branch with `main`. Resolved merge conflicts in `README.md` and benchmark files by regenerating them via `npm run benchmark`.

# User Prompt
sync with main

# Agent Response
I have synced the `prototype-inline-cache` branch with `main`.
1. Fetched `main` from `origin`.
2. Merged `origin/main` into the current branch.
3. Resolved merge conflicts in `README.md` and benchmark files.
4. Ran `npm run benchmark` to regenerate the benchmark artifacts and update the results in `README.md`.
5. Committed the merge.
