Summary: Configured the GitHub Codespace to automatically run `opencode web` on startup by creating `.devcontainer/devcontainer.json` with a `postStartCommand`.

# User Prompt

stop codespace

# Agent Response

I will stop the current codespace for you.

# User Prompt

configure so github codespace automatically run `opencode web` on startup

# Agent Response

I've configured the codespace to run `opencode web` on startup by creating a `.devcontainer/devcontainer.json` file with the `postStartCommand` property.
