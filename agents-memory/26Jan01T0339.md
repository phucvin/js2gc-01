Summary: Created a WAT example demonstrating a map from i32 to Point2d struct using open addressing hash map.

# User Prompt

add wat example for a map from i32 to struct of point2d (2 i32), do some operations on that map, and return the map size in main

also follow agents.md

# Agent Response

I will create a WAT example `wat_examples/map.wat` that implements a simple hash map using linear probing.

**Plan:**

1.  **Create Session File**: `agents-memory/26Jan01T0339.md`.
2.  **Create WAT Example**: `wat_examples/map.wat` with `Point2d` struct, `Entry` struct, and `Map` logic (put, get, size).
3.  **Run Tests**: Verify using `wat_examples/run.ts`.
4.  **Submit**.
